#include <iostream>
#include <cmath>
#include <stdio.h>

using namespace std;

int main()
{
    int a, b, c1, d1, n, x;
    x = 64;
    cin >> a >> b;
    cout << (a & b);
    return 0;
}