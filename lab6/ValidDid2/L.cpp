#include <bits/stdc++.h>
using namespace std;

bool valid(string S, int N)
{
    if(N<0)
        return;
    if(S[N]>=48 && S[N]<=57)
        
}

int main()
{
    string s;
    int n;
    cin>>s>>n;
    cout<<(valid(s,n)?"Valid":"Not valid";
    return 0;
}